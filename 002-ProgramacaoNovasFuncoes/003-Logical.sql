USE MASTER

IF EXISTS(SELECT * FROM SYS.DATABASES WHERE NAME='DEMO')
	DROP DATABASE DEMO

CREATE DATABASE DEMO
GO

USE DEMO

CREATE TABLE DBO.PESSOA
(
    ID INT PRIMARY KEY IDENTITY, 
	NOME VARCHAR(50), 
	SEXO CHAR(1)
)

INSERT 
	DBO.PESSOA 
VALUES 
	('ADÃO', 'M'), 
	('EVA', 'F'), 
	('COBRA', '?'), 
	('DERCY', NULL)

SELECT 
	ID, 
	NOME, 
	IIF(SEXO = 'M', 'MASCULINO', IIF(SEXO = 'F', 'FEMININO', 'NÃO DEFINIDO')) AS SEXO
FROM 
	DBO.PESSOA

SELECT 
	ID, 
	CHOOSE(ID % 2 + 1, 'PAR', 'ÍMPAR')
FROM
	PESSOA
