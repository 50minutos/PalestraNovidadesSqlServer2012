USE MASTER

IF EXISTS(SELECT * FROM SYS.DATABASES WHERE NAME='DEMO')
	DROP DATABASE DEMO

CREATE DATABASE DEMO
GO

USE DEMO

CREATE TABLE DBO.PESSOA
(
    ID INT PRIMARY KEY, 
	NOME VARCHAR(50)
)

CREATE SEQUENCE SEQID AS INT
    START WITH 1 
    INCREMENT BY 1

INSERT 
	DBO.PESSOA 
VALUES 
	(NEXT VALUE FOR SEQID, 'ADÃO'), 
	(NEXT VALUE FOR SEQID, 'EVA')

SELECT 
	* 
FROM 
	DBO.PESSOA

SELECT 
	* 
FROM 
	SYS.SEQUENCES
